# Maintenance state (system-level, committed).
#
# Travels with ai-system in git so any machine/CI can see when the system
# was last maintained and what was found (cross-machine continuity).
#
# Scope: SYSTEM-LEVEL ONLY (metrics / tool gates / proposals / fixes).
# Machine / environment observations go to the per-run diagnostic log
# (logs/, gitignored), NOT here. Triage rule + trigger keywords live in
# cli/commands/aic-maintain.md (Outputs / state-update section).
#
# Write path (AI-manual): read next_maintenance at session start (ADR-0009);
# write last_run / mode / next_maintenance / last_findings at run end.

last_run: 2026-09-11
mode: on-demand
next_maintenance: '2026-09-16'   # weekly 节奏：+7d（本次 on-demand 专项，不推进计划内下次）
last_findings:
- on-demand 专项：按今日 code-review 日志 review code-review 流程（scope 指定）；工具门禁全绿（lint 0/0/25、path 715 refs 0 broken、workflow-command-audit 0/0、proposal-audit 0 gate error、quick-check OK）
- 指标：Skills 32→30（repo-metrics 口径，apply-openspec/archive-openspec 随 639494b 移除）；Workflows 15 / RFC 14 / Governance 59 / Templates 22 持平；implement +46 / repository-maintainer +26 / review +6 为近 2 日流程修订
- 核心发现：M2 runtime-code-review「不修改业务实现」声明 vs spec-comparison 实际执行 fix+merge+push+wiki（今日三仓推送 origin）自相矛盾 → **已修复（用户确认）**：声明改为「默认 review-only、显式授权可扩展」+ 新增 Spec-Comparison Fix-Apply Extension 阶段（Sync→Fix-Apply→Build/Check→Commit&Push→Record，授权规则：未授权永不运行 + L1 登记 + 未解决项不静默实施）；L1 HotFix loop wiki 行为 → **已同步**（远程 wiki 用户自管，agent 仅写本地 change-summary-wiki.md）；M1 code-review.md 节顺序违规已修复（用户 L2 批准：Target Branch Resolution→Context 子节、Spec-Comparison Review Mode→Outputs 子节，内容逐字保留，门禁全绿）；M3（相邻 review/verify）T-011 重跑覆盖 2026-09-10 verification-report（workspaces 无 git 不可恢复）→ 建议写报告前目标文件冲突检查（待确认）
- 交互层评估（执行建议 1 前置）：aic code-review 菜单交互链路（menu flow_analysis → wizard frontmatter 字段收集 → prompt_builder → opencode 会话）评估完成，4 项发现：P1 i18n 缺口（Target Theme/Confluence Spec Page Id 无 field_note；Output Directory 备注默认值错配）→ 已补齐（field_note/icon）+ Output Directory 输入已移除（错配消除）+ P2 分支解析双重询问 → 已减少（Branch Mapping 直接采用、Target Theme 唯一命中直接采用）；P3 review/code-review 条目易混淆 + P4 Target Theme/Branch Mapping 无动态候选 provider → 交互层结构建议待变更管理；Review Focus 优化已落地（10 预置候选 + 多选 + 默认全面审查）；skill 加载缺陷已修复（F1：extension SKILL.md description 块标量 `>` 误解析 → 改用共享 YAML 解析器 cli/services/frontmatter.py，扫描 0 残留、全 CLI 单测 259 OK）；可配置技能源目录已落地（F2：skill-groups.yaml 新增 skill_roots 配置驱动发现，先配置 core+extensions；core_skills 白名单退役；core 组 2→29 全量可达，extensions 未登记回退 env layers.skills）；skill 菜单分组高亮（cyan 系配色统一）+ 每屏 10 项可筛选 + 长描述截断 50 字符/预览全文（F3：section 主题色 + max_visible=10 + _skill_label 截断）；aic 向导项目菜单置顶无项目入口（F4：system/AI 引导 → 前两项，索引偏移同步）；skill 菜单选中行黑字白底、非选中无背景、每屏 20 项、公司技能组提前一级（F5 修订 3）；非开发主链菜单体检（F7：env-init 重复字段修复 + 字段备注/图标 0 缺失 + 交互层英文文案已本地化 A3）；chain 链路无项目无用问题已修复（F8：required 链强制项目留空=取消 + wizard 项目透传 launcher）；aic 菜单输入中文过滤崩溃已修复（F6：_read_raw UTF-8 lead byte 补齐续字节 + latin-1 回退，+4 回归单测 263 OK）
- 一致性抽查 8 项：6 过 / 2 失败（workflows 八段=code-review.md、doc-vs-reality=code-review runtime 声明），其余（注册表最小化/链接/状态卫生/run-log 覆盖/提案）全过；ai-system git 干净
- 提案：P41/P42/P46 保持 defer 季度回顾；P26/P28 action items 无新触发；无新增提案
- 报告索引：MAINTENANCE-2026-09-11.md 已补登 reports/README.md；语言门禁 PASS（cjk 0.35）
- 本专项 read-first：除已批准执行的建议 1（L2 节归位）+ 建议 2（用户确认的 runtime/loop 文档修正）外未做其他就地修改，其余输出建议待用户决策
  （runtime-develop.md Stage 6 gates 段 7 行 + workflows/develop.md 存量 3 行）；自查范围结论：
  yaml desc/工具注释/报告中文惯例合法保留；LANGUAGE_CONVENTION 中文为规范自身示例与简体要求；
  用户可见输出遵循系统语言 zh；运行时语言门禁由 runtime-base Complete 步骤执行（P45）
- 工作区迁移 WSL（9p 双视图根因解决，用户主导方案 A）：全工作区迁 WSL 文件系统单视图（9p 读缓存假象/false samefile/stat 假死/git add 漏扫根治）；git 仓重新 clone 最新 + 可复制内容 rsync（排除 worktrees/.git/工具状态）；projects 为符号链接指向业务仓物理（87 仓未迁、Windows 工作流保持、归档仓 ext4 副本为日常/D 盘旧仓备份）；worktree 业务仓 4 个 git 链不受影响；路径适配（archive_sync 常量/模板占位化/公开仓不携带本机路径）；aic（pip editable）指向修复（finder+dist-info）；develop 前置规则固化：无 Dev Setup 先跑 dev-setup 禁止自推/跳步（workflows/develop.md，提交 c7c6496）；完成报告目录约定 completion-reports/（runtime-develop Location 对齐 da469ab）；验证基线：unittest 178 OK / lint 0/0 / 归档 dry-run 0 / 三仓 M=0；详细运维日志（含本机路径）在 logs/（gitignored 不公开）
- 待办（2026-09-03 定案）：P46(b)「验证状态核验」推荐实施——review/verify 无验证标记形式化
  检查（runtime-verify 0 相关步骤），编译阻断→验证义务传递靠文本约定；维护窗口实施：
  runtime-review/runtime-verify 新增「验证状态核验」步骤（检查开发产物/任务卡 compile/unittest
  验证标记，WARN 级，不阻断）+ 完成报告可选「验证状态」字段（对齐 P47 completion-reports 约定）；
  与 P46(a)/P41/P42（tr5 家族）解耦独立；模板改动不影响进行中会话（后续会话生效）
- 待办（2026-09-04 定案）：L2 配置对象化（季度窗口）——同类 @Value 收敛为
  @ConfigurationProperties 配置 POJO（默认值集中 POJO 字段一处、new Properties() 自带默认供
  测试直构、字段 final 杜绝双写），根治默认多源；新代码默认范式写入 spring.md；
  前置 L1 已落地（spring.md §Configuration Injection + format-check 第 8 项，36d19e9）；
  批尺寸防御校验（batchSize<=0 拦截）纳入存量修复清单
- 试点基线回退（业务侧决策，2026-09-04）：platform-api 本地 master 回退 775f09e4/60670b450
  （style 格式基线 + checkstyle 资产）→ 0d594b55f（8-21），本地=远端（ahead 0）；原因=业务侧
  未采纳基线（master 提交纯格式优化顾虑）。影响：C2 门禁在开发改动时回归全量差分（需 --skip），
  checkstyle 因仓内无资产 SKIP（资产缺失语义）。可逆：reflog（60670b450）/远端 style 分支
  （suppressions 4845 完整副本）/format-baseline 一键重跑。脚本语义验证（回退实验）：增量无改动
  三连 gate 全 PASS（存量不可见）；checkstyle-gate 资产缺失 SKIP（对齐模板语义，9656c3e）
- Windows 侧 ai-system 逐步移除（2026-09-04）：D 盘 ai-workspace/ai-system → ai-system-bak（观察）；
  ai.bat 入口废弃删除（用户确认主链全在 WSL，Windows 不再启动 aic）。前提核查：D 盘副本落后
  （aaadbc2 < WSL 3476486）无备份价值（git 远端即备份）、0 未提交/未推送；projects symlink 指向
  D 盘 project-resources 不受影响。观察期后无回归 → 正式删除 ai-system-bak。业务仓 Windows 工作流
  （IDEA/Maven）保持 D 盘物理
- GitHub Actions CI 移除（2026-09-04）：删除 workflows 下的 CI workflow 文件。理由：ai-system 为本地维护
  工具仓（主链 WSL），CI 与本地 check.py 门禁重复；GitHub Actions 环境（ubuntu、无 ~/.local/jre17、
  无本机配置）与全量 unittest 结构性错配 → 经常性报错（此前 D701ce2 修 JRE 印证）；无外部协作价值。
  回归闸回归到本地 check.py + 全量单测（每次提交前跑）。如需恢复，该 workflow 文件在 git 历史可随时找回。
- 语言门禁 L2 实验结论（2026-09-05，周巡检驱动）：「机制级静态语言门禁」经实验**判定不可行**
  ——英文纪律区（templates/runtime + workflows）逐行 CJK 检测 134 处，全为合理中文（用户可见报告
  模板标题、团队术语「转测文档」、yaml 字段注释、行尾注释、命名格式「概述-用户名」）；整行纯中文
  =0。混合语言生态（执行提示词英文 + 术语/注释/报告中文惯例）无法用静态逐行检查区分 → 静态门禁
  必然误报（2 次实验证明）。语言合规机制维持：P45 运行时门禁（language-gate.py，用户可见文本）
  + 语言自查惯例（b5b09b0/4c0125a）；不引入静态逐行 CJK 门禁。本次顺手修复 runtime-develop
  L157-161 真中文（d28eb20 引入的增量说明→英文）。
- 存量债基线登记（2026-09-05，周巡检 + 用户决策 A）：format-check 全量扫活跃 worktree——
  knowledge-api 171 FAIL/3147 WARN + 日志中文 17 文件；resource-manager 10 FAIL/242 WARN +
  日志中文 0（@Value 会话已处理过）。处理：**基线化**（不海量修改业务仓——大 diff/干扰任务/
  评审成本 > 价值）；门禁 --changed 增量语义复验通过（存量仓无改动 PASS、新增行精确拦截，
  存量豁免+新增拦截机制成立，与 checkstyle suppressions 同模式）。存量债保持登记，
  随任务开发自然消减。
