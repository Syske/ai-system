# Maintenance state (system-level, committed).
#
# Travels with ai-system in git so any machine/CI can see when the system
# was last maintained and what was found (cross-machine continuity).
#
# Scope: SYSTEM-LEVEL ONLY (metrics / tool gates / proposals / fixes).
# Machine / environment observations go to the per-run diagnostic log
# (logs/, gitignored), NOT here. Triage rule + trigger keywords live in
# cli/commands/aic-maintain.md (Outputs / state-update section).
#
# Write path (AI-manual): read next_maintenance at session start (ADR-0009);
# write last_run / mode / next_maintenance / last_findings at run end.

last_run: 2026-08-31
mode: weekly
next_maintenance: '2026-09-07'   # weekly +7d
last_findings:
- maintain-delta: NO_CHANGES（2026-08-25 后无提交），跳过全量审计
- quick-check OK(0)，近 7 日趋势稳定（8/25 ISSUES 已修复回落）
- code-review.md 两个可选节插入必选节之间（Target Branch Resolution / Spec-Comparison Review Mode），需走 OPERATIONS §11 结构调整
- reports/README.md 缺 MAINTENANCE-2026-08-25.md 和 MAINTENANCE-2026-08-26.md 索引登记
- 7 项 Proposed 提案开放（P28/P35/P36/P37/P41-P43），季度回顾时集中决策
- 工具门禁：0 BLOCKER / 0 ERROR / 25 WARN（frontmatter 校验），path-audit OK，extensions-lint OK
- 代码质量：31 skills / 15 workflows / 14 RFC / 57 governance / 22 templates，指标全部持平
- 机器级观察（python shim CRLF 问题等）见 logs/ per-run diagnostic-log，不入此文件
