# Maintenance state (system-level, committed).
#
# Travels with ai-system in git so any machine/CI can see when the system
# was last maintained and what was found (cross-machine continuity).
#
# Scope: SYSTEM-LEVEL ONLY (metrics / tool gates / proposals / fixes).
# Machine / environment observations go to the per-run diagnostic log
# (logs/, gitignored), NOT here. Triage rule + trigger keywords live in
# cli/commands/aic-maintain.md (Outputs / state-update section).
#
# Write path (AI-manual): read next_maintenance at session start (ADR-0009);
# write last_run / mode / next_maintenance / last_findings at run end.

last_run: 2026-09-01
mode: on-demand
next_maintenance: '2026-09-07'   # weekly +7d（考量：本次为 on-demand/语言边界专项，周维护节奏保持）
last_findings:
- 语言边界专项（Scope=workflows）：定位 dev-setup/maintain 交互英文根因为「执行层未按 system language=zh 转换」，非配置回退（locale 仍 zh，配置干净）
- pilot A 落地：aic-maintain.md 追加「交互语言=系统语言(zh)」强制约束；PromptBuilder 自测通过，交互实测中文呈现
- 延后项：将「交互按 locale」约束收敛进全局规范（方案 C，走 OPERATIONS §11）；运行时通用机制（B）待 pilot 结论
- 提案：6 开放（P28/P35/P36/P37/P41/P42），季度回顾集中决策（与上期一致，本轮未决）
- 工具门禁：0 BLOCKER / 0 ERROR / 25 WARN；path-audit OK；workflow-command-audit 0 blocker（aic-maintain thin-command 1 WARN 提示性）
- 机器级观察（python shim 用 python3 等）见 logs/ per-run diagnostic-log，不入此文件
