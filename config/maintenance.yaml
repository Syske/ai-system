# Maintenance state (system-level, committed).
#
# Travels with ai-system in git so any machine/CI can see when the system
# was last maintained and what was found (cross-machine continuity).
#
# Scope: SYSTEM-LEVEL ONLY (metrics / tool gates / proposals / fixes).
# Machine / environment observations go to the per-run diagnostic log
# (logs/, gitignored), NOT here. Triage rule + trigger keywords live in
# cli/commands/aic-maintain.md (Outputs / state-update section).
#
# Write path (AI-manual): read next_maintenance at session start (ADR-0009);
# write last_run / mode / next_maintenance / last_findings at run end.

last_run: 2026-09-03
mode: on-demand
next_maintenance: '2026-09-07'   # 周节奏锚点保持；09-03 on-demand 为 pi-lens 扩展评估专项（FIRST_RUN 全量审计顺延至本次窗口）
last_findings:
- pi-lens 扩展评估（on-demand 专项）：结论「现在不安装」已获用户确认（2026-09-03）——与 Stage-6 门禁链能力重叠、google-java-format 与 IDEA profile/spotless 双格式权威冲突、自主 reformat/autofix 绕过变更控制、git-guard 会因存量债阻塞提交；重估触发条件与缓解底线已入 MAINTENANCE-2026-09-03.md §7；机器级观察见 logs/ 诊断日志；P47/本报告 README 索引已补登（WARN 归零）；衍生场景「env-init 首次运行触发」经用户指示增补进 P36（触发层 T-b 方案，内容层先行）不另立提案
- 最新改动诊断：3580bb0（pilot A）流程规范；孤儿改动 templates/runtime/runtime-base.md 经确认后按用户决策 B3 并入 P45 Phase 1 正式化（完成报告语言声明 + 语言门禁步骤）——流程闭环
- README 索引 WARN 已修复：MAINTENANCE-2026-09-01.md 与本次报告补登 reports/README.md（proposal-policy §6），check.py WARN 4→3
- 工具门禁：0 BLOCKER / 0 ERROR / 25 WARN；path-audit 688 refs OK；workflow-command-audit 0 blocker；proposal-audit 0 gate error，索引刷新 38 提案
- 提案：P35、P45 已 Implemented；开放 5（P28/P36/P37/P41/P42）defer 季度回顾；4 个 action items（P26×2/P28×2）保持开放
- code-review.md 节顺序违规仍开放（08-31 已知，需 L2 审批）；机器/环境观察见 logs/ per-run diagnostic-log，不入此文件
- L1 语言声明优化已实施（确认后执行）：LANGUAGE_CONVENTION v1.1（用户面向输出 SHOULD→MUST 跟随系统语言；新增 system language 单一事实源；self-check 扩展语言选择+简体两层；交互约束收敛回约定）+ AI_OPERATING_RULES v1.5（§Language Boundary 入口收敛点）+ aic-maintain pilot A 改引用式；gate 全绿（lint 0/0/25、path OK、check 3 提示 WARN）；机制级语言门禁（L2）仍待 OPERATIONS §11 提案
- P35 已实施（用户授权）：9 文件命令文档统一 python3、shebang/subprocess 核对零改动、env.yaml 未动；验证全绿（check PASS 3 提示 WARN、lint 0/0/25、path OK、unittest 164 OK、quick-check OK）；状态 → Implemented，PROPOSALS/README 索引已同步
- P45 已实施（用户批准，B3 并入）：runtime-base 语言门禁步骤（PASS/WARN/FAIL 三步 + diag log 记录）+ 新工具 language-gate.py（启发式 CJK 占比，阈值 20/10，双语标题白名单，exit 0/1/2）+ tools/README.md 登记 + LANGUAGE_CONVENTION v1.2/AI_OPERATING_RULES v1.6（self-check 升级运行门禁）+ aic-maintain 试点链接入；验证：单元样例 3 类通过 + 历史回归 5 文档 PASS（0.28-0.56）+ 全量 gate 绿（lint 0/0/25、path OK、check PASS 3 提示、unittest 164 OK、quick-check OK）；R1 补丁（运行日志核查驱动）：发现 Extends 无加载器合并、门禁步骤对 AI 不可见 → prompt_builder 新增 _merge_runtime_base（base @keep 机制并入骨架）+ base 门禁段单行 @keep + 回归单测（unittest 164→165 OK）；另落 aic-maintain「Run-log coverage」巡检检查项；#5 核查：review/verify 无未验证标记形式化检查（缺口留建议）
- P46 已起草（待审批，日志全扫描驱动）：tr5 发布债收口（check_tr5 8 FAIL：TR3 引用/缺 SVG/§0 标注/题图行粘连，随线上 TR5 update 执行）+ review/verify 验证标记检查（review WARN 级 / verify 阻塞级）；与 P41/P42 联动声明；待 Review Log 用户决策
- A 层格式自检工具已交付（用户问询驱动）：tools/format-check.py（纯 python3 无 JDK——单行 Javadoc/中文方法名/注释 T-xxx 泄漏/Map payload(main)/4 空格比例；--changed 仅查本 change、--check-commit；目录防护）；接入 runtime-develop Formatting gate；单测 171 OK；存量债全量扫 34 FAIL/149 WARN 待批量治理；B 层 spotless（业务仓 pom）草稿已交付转业务侧
- C2 eclipse JDT 干跑门禁已交付（用户选型 + 环境感知需求）：tools/jdt-format-gate/（JdtFormatCheck wrapper + eclipse-format.xml 初始 tab=4、12 jar 运行闭包）+ format-jdt-gate.py（JDK 自动探测/用户提供，组件缺失交互授权 setup/skip/abort，--batch 需 --skip，配置持久化 env.yaml runtime.jdt.*；exit 0/1/2/3）；runtime-develop 接入；单测 176 OK；实测样例正确，真实 worktree 1010/1690 文件 differ＝存量与初始 profile 差距待 IDEA profile 校准
- 日志内容规范已补（用户报告驱动）：新写 log.error（CoolBsDataHandler:168「落 bs_fail_task 失败，跳过该条」）中文 + 口语指代违规 → documentation.md 新增 Log Content（error/warn 及新写 info 英文；语义模板动作+对象+后果+参数；末参带异常）+ format-check #6 检查（error/warn 中文 FAIL、info 中文 WARN，实测精确检出）+ checklists 项 + 单测 178 OK；全仓存量 53 处中文 error/warn log 待批量清理
- IPD 周期归档方案落地（用户需求 + junction 事故复盘）：junction 嵌 workspace 会破坏主链（git 边界渗入/状态耦合）→ 改单向快照（workspace 唯一维护源）；extensions/archive-ipd-workspace（SKILL + archive_sync.py：白名单 copy→git add 目录→commit→push，Windows python 执行绕 drvfs 读缓存怪癖）；main-chain-capabilities.yaml release 段注册扩展点（更新时推荐执行）；归档仓已入库推送（迭代项目产物：openspec 40/tr5 7 等，gitignore 补 tr5 xml 例外）；教训入 OPTIMIZATION_LOG
- task-splitter 分支占位符修复（用户检查驱动）：202610-cool-italent-sync-plus 24/24 任务卡 branch 字段 `{date}` 未替换（半渲染，desc/service 已填）→ 存量批量补全为 20260820（投影 project-context 冻结分支名，0 残留）+ workflow.md 补 date 写入规则（已建分支写全名/未建写需求确认日/禁占位符）+ Verify 强化；门禁全绿
- 代码注释/提交任务编号泄漏规范（用户报告驱动）：develop 产出 9+ 处 Javadoc 带 `（T-001）` 等 + 2 条提交标题以 T-xxx 开头 → 根因 = traceability 误读（溯源应存 Task Card 非代码注释）+ documentation.md 无内容对象约束 → 新增 Comment Content / Commit Content 规范 + implement checklists 检查项；存量 worktree 清理待用户决策
- 语言规范门禁自查（用户提醒驱动，2026-09-03）：本轮模板改动混入中文 → 已按模板英文纪律修复
  （runtime-develop.md Stage 6 gates 段 7 行 + workflows/develop.md 存量 3 行）；自查范围结论：
  yaml desc/工具注释/报告中文惯例合法保留；LANGUAGE_CONVENTION 中文为规范自身示例与简体要求；
  用户可见输出遵循系统语言 zh；运行时语言门禁由 runtime-base Complete 步骤执行（P45）
- 工作区迁移 WSL（9p 双视图根因解决，用户主导方案 A）：全工作区迁 WSL 文件系统单视图（9p 读缓存假象/false samefile/stat 假死/git add 漏扫根治）；git 仓重新 clone 最新 + 可复制内容 rsync（排除 worktrees/.git/工具状态）；projects 为符号链接指向业务仓物理（87 仓未迁、Windows 工作流保持、归档仓 ext4 副本为日常/D 盘旧仓备份）；worktree 业务仓 4 个 git 链不受影响；路径适配（archive_sync 常量/模板占位化/公开仓不携带本机路径）；aic（pip editable）指向修复（finder+dist-info）；develop 前置规则固化：无 Dev Setup 先跑 dev-setup 禁止自推/跳步（workflows/develop.md，提交 c7c6496）；完成报告目录约定 completion-reports/（runtime-develop Location 对齐 da469ab）；验证基线：unittest 178 OK / lint 0/0 / 归档 dry-run 0 / 三仓 M=0；详细运维日志（含本机路径）在 logs/（gitignored 不公开）
- 待办（2026-09-03 定案）：P46(b)「验证状态核验」推荐实施——review/verify 无验证标记形式化
  检查（runtime-verify 0 相关步骤），编译阻断→验证义务传递靠文本约定；维护窗口实施：
  runtime-review/runtime-verify 新增「验证状态核验」步骤（检查开发产物/任务卡 compile/unittest
  验证标记，WARN 级，不阻断）+ 完成报告可选「验证状态」字段（对齐 P47 completion-reports 约定）；
  与 P46(a)/P41/P42（tr5 家族）解耦独立；模板改动不影响进行中会话（后续会话生效）
- 待办（2026-09-04 定案）：L2 配置对象化（季度窗口）——同类 @Value 收敛为
  @ConfigurationProperties 配置 POJO（默认值集中 POJO 字段一处、new Properties() 自带默认供
  测试直构、字段 final 杜绝双写），根治默认多源；新代码默认范式写入 spring.md；
  前置 L1 已落地（spring.md §Configuration Injection + format-check 第 8 项，36d19e9）；
  批尺寸防御校验（batchSize<=0 拦截）纳入存量修复清单
- 试点基线回退（业务侧决策，2026-09-04）：platform-api 本地 master 回退 775f09e4/60670b450
  （style 格式基线 + checkstyle 资产）→ 0d594b55f（8-21），本地=远端（ahead 0）；原因=业务侧
  未采纳基线（master 提交纯格式优化顾虑）。影响：C2 门禁在开发改动时回归全量差分（需 --skip），
  checkstyle 因仓内无资产 SKIP（资产缺失语义）。可逆：reflog（60670b450）/远端 style 分支
  （suppressions 4845 完整副本）/format-baseline 一键重跑。脚本语义验证（回退实验）：增量无改动
  三连 gate 全 PASS（存量不可见）；checkstyle-gate 资产缺失 SKIP（对齐模板语义，9656c3e）
- Windows 侧 ai-system 逐步移除（2026-09-04）：D 盘 ai-workspace/ai-system → ai-system-bak（观察）；
  ai.bat 入口废弃删除（用户确认主链全在 WSL，Windows 不再启动 aic）。前提核查：D 盘副本落后
  （aaadbc2 < WSL 3476486）无备份价值（git 远端即备份）、0 未提交/未推送；projects symlink 指向
  D 盘 project-resources 不受影响。观察期后无回归 → 正式删除 ai-system-bak。业务仓 Windows 工作流
  （IDEA/Maven）保持 D 盘物理
- GitHub Actions CI 移除（2026-09-04）：删除 workflows 下的 CI workflow 文件。理由：ai-system 为本地维护
  工具仓（主链 WSL），CI 与本地 check.py 门禁重复；GitHub Actions 环境（ubuntu、无 ~/.local/jre17、
  无本机配置）与全量 unittest 结构性错配 → 经常性报错（此前 D701ce2 修 JRE 印证）；无外部协作价值。
  回归闸回归到本地 check.py + 全量单测（每次提交前跑）。如需恢复，该 workflow 文件在 git 历史可随时找回。
