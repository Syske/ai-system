# Maintenance state (system-level, committed).
#
# Travels with ai-system in git so any machine/CI can see when the system
# was last maintained and what was found (cross-machine continuity).
#
# Scope: SYSTEM-LEVEL ONLY (metrics / tool gates / proposals / fixes).
# Machine / environment observations go to the per-run diagnostic log
# (logs/, gitignored), NOT here. Triage rule + trigger keywords live in
# cli/commands/aic-maintain.md (Outputs / state-update section).
#
# Write path (AI-manual): read next_maintenance at session start (ADR-0009);
# write last_run / mode / next_maintenance / last_findings at run end.

last_run: 2026-08-24
mode: on-demand
scope: workflows
next_maintenance: '2026-08-30'   # scheduled weekly unchanged (this was an ad-hoc on-demand run)
last_findings:
- 增量基线：maintain-delta FIRST_RUN → 全量审计；已 --record 基线 head=3237f8d（下次起增量判定生效）
- 工具：quick-check ISSUES(1)（详情见 per-run diagnostic-log，机器级不入此文件）；repo-lint BLOCKER/ERROR 0 / WARN 25（既有无新增）；
  path-audit 0 broken（665 refs）；workflow-command-audit 0 blocker/1 WARN（aic-maintain 125 行既有）；CLI 单测 OK
- 指标：skills 31/workflows 15/rfc 14/governance 56/templates 22 全不变；frontmatter 30 valid/1 missing（恒定）
- scope=workflows 一致性抽查：11 项 10 过/1 ⚠️（AGENTS.md 缺失——doc-vs-reality 检查对象不存在，L2 需用户决策：
  重建或检查项降级）；README 条件转移表缺 release BLOCKED→develop 行（L1 待确认）；workflows 域结构/注册表/
  引用链/Next 链全绿；3 个条件重入环均文档化
- 开放提案：P28（B/D defer 维持）；P26（2 条开放项 defer 维持）；P35（Proposed，python→python3 多文件，待用户决策）；P36（Proposed，setup.py 初始化完善，待用户决策）
- 修复批次：用户确认后实施 ① workflows/README.md 补 release BLOCKED→develop（L1）② 工作区根重建 AGENTS.md 简写版（L2，
  覆盖结构图/运行目录表/关键设计决策/单任务工时/主链 workspace-anchored）③ Path Anchor 英文化（LANGUAGE_CONVENTION）
  ④ 提示词不注入悬空 extensions 路径（_resolve_ref None + caps 跳过，用户澄清后实施）；复验全绿（repo-lint 0/0/25、
  path-audit 0 broken、CLI 152 测试 OK）；P35/P36 仍待用户决策
