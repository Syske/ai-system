# Migration Plan v2

> 基于 Repository Architecture Report v2 的可执行迁移方案
> 适用日期: 2026-07-03

---

## Phase 1: Runtime 职责清理

### Task 1.1: ai-system/runtime/ → ai-runtime/

```bash
# 在确认无进程引用后执行

# 创建目标目录
mkdir -p ai-runtime/opencode/adapters
mkdir -p ai-runtime/claude

# 迁移 adapter/executor 代码
move ai-system\runtime\index.js            ai-runtime\opencode\
move ai-system\runtime\opencode.js         ai-runtime\opencode\
move ai-system\runtime\agents\adapter.js   ai-runtime\opencode\adapters\agent.js
move ai-system\runtime\claude\adapter.js   ai-runtime\claude\adapter.js
move ai-system\runtime\opencode\adapter.js ai-runtime\opencode\adapter.js
move ai-system\runtime\opencode\executor.js ai-runtime\opencode\executor.js

# 清理空目录
Remove-Item ai-system\runtime -Recurse
```

### Task 1.2: 更新引用路径

搜索所有引用 `ai-system/runtime/` 的文件，更新为 `ai-runtime/`。

```bash
# 在 workspace 根目录执行
rg -l "ai-system/runtime" --type-not class
```

---

## Phase 2: 项目 Skill 去重

### Task 2.1: 确认权威来源最新

对比 `ai-system/skills/openspec-*` 与 `projects/openspec-test/.claude/skills/openspec-*` 的内容。

如果 ai-system 版本不是最新的，先从 projects 同步，再删除 projects 中的副本。

### Task 2.2: 删除 projects 中的重复 skill

```bash
Remove-Item projects\openspec-test\.claude\skills -Recurse
Remove-Item projects\openspec-test\.opencode\skills -Recurse
Remove-Item projects\pywechat-live-2608\openspec\.opencode\skills -Recurse
```

### Task 2.3: 建立 bootstrap 机制

在 `ai-system/cli/` 中添加 bootstrap 命令，使其在初始化项目时自动从 `ai-system/skills/` 引用技能。

```
ai-system/cli/
  bootstrap.js    ← 新增: 项目初始化时创建 alias/link 到 ai-system/skills/
```

对于 OpenCode 环境，使用 `.opencode.jsonc` 中的 `skills` 配置指向 `ai-system/skills/`:

```jsonc
{
  "skills": [
    "../ai-system/skills/openspec-apply-change",
    "../ai-system/skills/openspec-archive-change",
    "../ai-system/skills/openspec-explore",
    "../ai-system/skills/openspec-propose"
  ]
}
```

---

## Phase 3: syske-skills 整合

### Task 3.1: 内容差异分析

```bash
# 对比 karpathy-guidelines (两边都存在)
diff -rq ai-system/skills/karpathy-guidelines/ syske-skills/andrej-karpathy-skills/skills/karpathy-guidelines/
```

### Task 3.2: 迁移其他 skill

```bash
mkdir -p ai-system/skills/agent-browser
mkdir -p ai-system/skills/agent-debug-diagnosis
# ... (对其他每个 skill 重复)
```

### Task 3.3: 更新引用

搜索所有引用 `syske-skills/` 的配置文件和文档，更新为 `ai-system/skills/`。

---

## Phase 4: ai-runtime 清理

### Task 4.1: 清理空目录和歧义目录

```bash
Remove-Item ai-runtime\superpowers -Recurse   # 空目录，删除
Remove-Item ai-runtime\agents -Recurse         # 空目录，删除
Remove-Item ai-runtime\commands -Recurse       # 空目录，删除
```

### Task 4.2: 创建明确的目录结构

```bash
mkdir -p ai-runtime/sdk
mkdir -p ai-runtime/scripts
```

---

## 迁移顺序与依赖

```
Phase 1 (Runtime) ──→ Phase 4 (Runtime Cleanup)
     │
     ▼
Phase 2 (Projects) ──→ Phase 3 (syske-skills)
```

- Phase 1 和 Phase 4 应连续执行
- Phase 2 和 Phase 3 可以并行
- Phase 1 必须在 Phase 4 之前

---

## 验证清单

迁移完成后，运行以下验证：

- [ ] `ai-system/runtime/` 已删除
- [ ] `ai-runtime/` 结构符合规范
- [ ] `projects/*` 中无 skills/ 目录
- [ ] `syske-skills/` 已迁移/删除
- [ ] `ai-system/skills/` 包含所有 skill
- [ ] `ai-system/workflows/` 是唯一的 workflow 来源
- [ ] `ai-system/governance/` 是唯一的 governance 来源
- [ ] OpenCode 可以正常加载 ai-system/skills/ 中的技能
- [ ] Claude 可以正常加载 ai-system/skills/ 中的技能

---

*Migration Plan generated by Architecture Maintainer*
