# Workflow: Bootstrap

## Purpose

Load environment configuration and prepare execution context for all downstream workflows.

## Runtime

- templates/runtime/runtime-bootstrap.md

## Preconditions

- None. This is the entry point of the workflow chain.
- AI system dependencies installed (`pip install -e .` in ai-system)
- `config/environments/{environment}.yaml` may be generated by this workflow via `tools/setup.py` when missing

## Inputs

Required:

- None

Optional:

- Environment (default: local)

## Context

Load only:

- ai-system/config/environments/{environment}.yaml

When the environment configuration is missing, invoke `tools/setup.py` to provision it, then load.

Never load project repositories or workspace content in this workflow.

## Outputs

- Environment Context (workspace_root, repository_root, workspaces_root, ai_system_root, methodologies_root)
- Workspace Metadata (initialized, no project binding)

## Exit Criteria

Success:

- Environment Context resolved and persisted
- Workspace directory initialized

Stop:

- Environment configuration missing and not provisioned by tools/setup.py → report and stop

## Next

- prepare — on new change

Note: any workflow that requires Environment Context may follow bootstrap.
